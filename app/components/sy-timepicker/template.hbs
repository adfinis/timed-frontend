<input
  type="text"
  class="form-control"
  placeholder="--:--"
  maxlength=5
  name={{name}}
  disabled={{disabled}}
  value={{displayValue}}
  pattern={{pattern}}
  onkeydown={{action 'handleKeyDown'}}
  onchange={{action 'handleChange'}}
>
