<input
  type="text"
  class="form-control"
  placeholder="--:--"
  name={{name}}
  disabled={{disabled}}
  value={{displayValue}}
  pattern={{pattern}}
  onkeydown={{action 'handleKeyDown'}}
  oninput={{action 'handleInput'}}
>
