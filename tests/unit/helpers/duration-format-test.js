import { durationFormat } from 'timed/helpers/duration-format';
import { module, test } from 'qunit';

module('Unit | Helper | duration format');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = durationFormat([42]);
  assert.ok(result);
});
