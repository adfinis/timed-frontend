import { since } from 'timed/helpers/since';
import { module, test } from 'qunit';

module('Unit | Helper | since');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = since([42]);
  assert.ok(result);
});
